Hello,

A short note from AbuseRadar, an independent web-data observatory.

In our recent index of public pages, {domain} surfaces {hacklink_count} third-party links that do not fit the site's normal content profile, {content_category}.

Patterns like this commonly originate from a CMS plugin or template file that was altered outside the site's usual editorial flow.

What the index shows:
  - Page-source nodes that are styled to be invisible to regular visitors
  - Outbound references to third-party sites unrelated to the site's stated purpose
{evidence_block}
A technical bundle for the affected pages — screenshots, file paths, and the rendered DOM — is published at our research index, no sign-in required:

  {report_url}
{complaint_block}
This is an automated, one-off notice. No reply is expected; if a follow-up is useful, team@abuseradar.org is reachable.

— AbuseRadar Research
abuseradar.org

— Sent because {domain} surfaced in our open index. We do not maintain mailing lists.
